spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/imagelite
    username: postgres
    password: sft6033
  jpa:
    database: POSTGRESQL
    hibernate:
      ddl-auto: update
    show-sql: true